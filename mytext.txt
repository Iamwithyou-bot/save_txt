
# Beispiel-Aufruf (PowerShell), äquivalent im Python per subprocess:
..\envs\cnos-venv\Scripts\python.exe -m src.scripts.inference_custom `
  --template_dir "$PWD\tmp\cnos_templates" `
  --rgb_path "<PFAD_ZUM_BILD>" `
  --stability_score_thresh 0.5



7)


# (falls nicht aktiv) Wrapper-venv aktivieren
Set-Location $HOME\novel-pose\pose-live
.\.venv\Scripts\Activate.ps1

# Live starten
python .\live_gigapose.py


# 1) Sicherstellen, dass du im richtigen Ordner bist
Get-Location

# 2) Zielordner anlegen
$models = ".\gigaPose_datasets\datasets\myObject\models"
$poses  = ".\gigaPose_datasets\datasets\templates\myObject\object_poses"
New-Item -ItemType Directory -Force -Path $models | Out-Null
New-Item -ItemType Directory -Force -Path $poses  | Out-Null

# 3) Deine PLY + models_info.json an den erwarteten Ort kopieren
#    (Passe die QUELL-Pfade an, je nachdem wo deine Dateien aktuell liegen!)
Copy-Item ".\datasets\myObject\models\obj_000001.ply" $models -Force
Copy-Item ".\datasets\myObject\models_info.json"       ".\gigaPose_datasets\datasets\myObject\" -Force

# 4) object_poses-Datei korrekt benennen (beide Varianten anlegen, um alle Codepfade abzudecken)
#    Wenn du nur 000001.npy hast:
Copy-Item ".\gigaPose_datasets\datasets\templates\myObject\object_poses\000001.npy" `
          ".\gigaPose_datasets\datasets\templates\myObject\object_poses\obj_000001.npy" -Force
#    Falls sie woanders liegt, such sie:
# Get-ChildItem -Recurse -Include 000001.npy | Select-Object -First 5 FullName

# 5) Existenz prüfen
Test-Path ".\gigaPose_datasets\datasets\myObject\models\obj_000001.ply"
Test-Path ".\gigaPose_datasets\datasets\myObject\models_info.json"
Test-Path ".\gigaPose_datasets\datasets\templates\myObject\object_poses\000001.npy"
Test-Path ".\gigaPose_datasets\datasets\templates\myObject\object_poses\obj_000001.npy"

